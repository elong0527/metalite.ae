% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt.R
\name{round_half_away_from_zero}
\alias{round_half_away_from_zero}
\title{Round half away from zero}
\usage{
round_half_away_from_zero(x, digits = 0)
}
\arguments{
\item{x}{A numeric vector, matrix, or data frame.}

\item{digits}{Number of decimal places.}
}
\value{
An object of the same shape as \code{x} with rounded values.
}
\description{
Round numeric values to a given number of decimal places, with decimal
ties (e.g., 2.25 at \code{digits = 1}) rounded half away from zero
(i.e., 2.25 becomes 2.3 and -2.25 becomes -2.3). This follows the
\code{roundSAS} algorithm from \code{pharmaverse/tidytlg} and therefore differs
from base R \code{round()}, which rounds ties to an even digit.
Values that round to zero (including small negative values) return
positive zero, so formatted output never shows negative zero.
}
\examples{
round_half_away_from_zero(c(2.25, 2.35, -2.25), digits = 1)
}
